name: My Workflow
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Find and Replace
        uses: seripap/gha-find-replace@v1
        with:
          find: "page<"
          replace: "page!!!<"
